<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Liste des badges appellée depuis la fiche de l'employee -->
        <record model="ir.actions.act_window" id="is_badge_action2">
            <field name="context">{'default_employee': active_id, 'search_default_employee': active_id}</field>
            <field name="name">Badges</field>
            <field name="res_model">is.badge</field>
            <field name="view_mode">list,form</field>
        </record>

        <record id="is_view_employee_form" model="ir.ui.view">
            <field name="name">is.hr.employee.form</field>
            <field name="model">hr.employee</field>
            <field name="inherit_id" ref="hr.view_employee_form"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button" name="%(is_badge_action2)d" type="action" icon="badge">
                        <field string="Badge" name="is_badge_count" widget="statinfo" />
                    </button>
                </div>
                <field name="mobile_phone" position="attributes"><attribute name="invisible">1</attribute></field>
                <field name="parent_id"    position="attributes"><attribute name="invisible">1</attribute></field>
                <page name="work_information" position="after">
                    <page string="Pointage" name="is_pointage">
                        <group>
                            <group string="Paramètrage">
                                <field name="is_matricule"/>
                                <field name="is_categorie"/>
                                <field name="is_interimaire"/>
                            </group>
                            <group string="Horaires">
                                <field name="is_jour1"/>
                                <field name="is_jour2"/>
                                <field name="is_jour3"/>
                                <field name="is_jour4"/>
                                <field name="is_jour5"/>
                                <field name="is_jour6"/>
                                <field name="is_jour7"/>
                            </group>
                        </group>
                    </page>
                </page>
            </field>
        </record>

        <record id="is_view_employee_tree" model="ir.ui.view">
            <field name="name">is.hr.employee.tree</field>
            <field name="model">hr.employee</field>
            <field name="inherit_id" ref="hr.view_employee_tree"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="is_matricule" optional="show"/>
                    <field name="is_categorie" optional="hide"/>
                    <field name="is_interimaire" optional="hide"/>
                    <field name="is_jour1" optional="hide"/>
                    <field name="is_jour2" optional="hide"/>
                    <field name="is_jour3" optional="hide"/>
                    <field name="is_jour4" optional="hide"/>
                    <field name="is_jour5" optional="hide"/>
                    <field name="is_jour6" optional="hide"/>
                    <field name="is_jour7" optional="hide"/>
                </field>
                <field name="work_phone" position="attributes"><attribute name="optional">hide</attribute></field>
                <field name="parent_id"  position="attributes"><attribute name="optional">hide</attribute></field>
            </field>
        </record>

        <record id="is_view_employee_filter" model="ir.ui.view">
            <field name="name">is_view_employee_filter</field>
            <field name="model">hr.employee</field>
            <field name="inherit_id" ref="hr.view_employee_filter"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="is_matricule"/>
                    <field name="is_categorie"/>
                    <field name="is_interimaire"/>
                </field>
            </field>
        </record>
    </data>
</odoo>
